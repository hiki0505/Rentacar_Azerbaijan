Rentacar website

Preferences:

1) Username + phone with phone as main field and username as required, is company boolean
2) In accounts.api usage of session variables is disabled
3) JWT implemented (djangorestframework_jwt)
4) Readded modelnames as there are no more duplicates and problem with slugfield adding is resolved
5) moderate feature turned off (commented)